# Server Configuration
server.port=8080
server.servlet.context-path=

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/J-Bookhub}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:120224}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Application Name
spring.application.name=bookstore-api

# Logging
logging.level.root=INFO
logging.level.com.bookstore=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration (10 minutes = 600000 milliseconds)
jwt.secret=${JWT_SECRET:your-secret-key-change-this-in-production}
jwt.expiration=${JWT_EXPIRATION:600000}

# VNPay Configuration
vnpay.tmnCode=${VNPAY_TMN_CODE:TESTMERCHANT}
vnpay.hashSecret=${VNPAY_HASH_SECRET:TESTSECRET}
vnpay.apiUrl=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
vnpay.apiQueryUrl=https://sandbox.vnpayment.vn/merchant_webapi/api/transaction

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:def4abpgi}
cloudinary.api-key=${CLOUDINARY_API_KEY:182333839669878}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:ooupF5SSAvpyQIyHeRyHQccCX2k}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB